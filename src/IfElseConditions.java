public class IfElseConditions {
    public static void main(String[] args) {
        // Command + alt + o : formatting
        // Operator
        // ==   : Equals
        // >    : Greater then
        // <    : Smaller then
        // <=   : less then equals
        // >=   : greater then equals
        // &&   : AND Used for combining two conditions
        // ||   : OR
        // !    : NOT
        // !=   : NOT EQUALS

        // Check if a number is greater then 10
//        int a = 5;
        int a = 10;

        // IF ELSE ladder

/*      if (a == 10) {
            System.out.println("number is equals to 10");
        } else if (a > 10) {
            System.out.println("number is greater then 10");
        } else {
            System.out.println("number is less then 10");
        }*/
        // SYNTAX:
        /*if(condition){ //condition will always be boolean data type
            // True block
        }else {
            // false block
        }*/

/*        int b =1;
        if (b == b) {
            System.out.println("Hello");
        } else {
            System.out.println("Hi");
        }*/

        System.out.println(1 == 1);
        if (false) {
            System.out.println("Hello");
        } else if (false) {
            System.out.println("Hi");
        }
        /**
         * WAJP to check if a number is even or not.
         * WAJP to check if a number is odd or not.
         * WAJP to check if a number is negative or not.
         * WAJP to check if a number is positive or not.
         */
    }
}